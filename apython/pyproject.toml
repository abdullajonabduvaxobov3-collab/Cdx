[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "uzbekpy"
version = "1.0.0"
description = "Useful Python tools"
readme = "README.md"
requires-python = ">=3.8"

authors = [
    {name = "Abdullajon"}
]